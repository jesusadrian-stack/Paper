<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /Paper/public/

    # Redirigir si no es un archivo o directorio real
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?r=$1 [QSA,L]
</IfModule>
